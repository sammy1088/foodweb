<div id="wrap"><center>
  <% title(@user.username) %>
  <br>
    <% if @user.image? %>
  <%= image_tag @user.image.url(:large), :class => "img-rounded" %>
  <% end %>
  <h1><%= @user.username %></h1>
  <% if @user.phone.present? %>
  phone: <b><%= @user.phone %></b><br><br>
  <% end %>

  <% if current_user == @user %><br>
<%= link_to "Edit account", edit_user_registration_path %>
  <% end %>
  <h2>Produce Selling</h2> 

  <div class="row">
     <div class="col-md-12">

       <% @user.posts.each do |post| %>
   
       <div class="col-md-6">
           <div id="indivs">
              <% if post.image? %>
 <%= image_tag post.image.url(:large), :class => "img-rounded" %>
              <br>
             <% else %>
             <%= image_tag "noimage.png", :class => "img-rounded", size: "200" %>
             
             <br>
<% end %>


            
      
             <%= link_to post.title, post_path(post) %><br> 
           

         </div>
           
            </div>
    
           <% end %>
              </div>
           
         
    

      
  
 </div>

  
  

</center>